<h2>Categories</h2>

<ul>
  <% @categories.each do |category| %>
    <li>
      <%= category.title %>
      <%= link_to "Delete", category_path(category), method: :delete %>
    </li>
  <% end %>
</ul>
