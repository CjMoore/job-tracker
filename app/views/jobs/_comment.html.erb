<div>
  <h5>Comment by <%= comment.author %></h5>
  <p class="comment"><%= comment.content %></p>
  <p>
    Posted <%= distance_of_time_in_words(comment.job.created_at, comment.created_at) %> later
  </p>
</div>
